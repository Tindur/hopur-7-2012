<!--@if(Request.IsAuthenticated) {
    <text><strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Útskrá", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Innskrá", "LogOn", "Account") ]
}-->

<div class="btn-group pull-right">
    <li class="dropdown" id="menu1">
            <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
              <i class="icon-user"></i> @User.Identity.Name
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>@Html.ActionLink("Breyta", "ProfileManagement", "Account")</li>
              <li class="divider"></li>
              <li>@Html.ActionLink("Útskrá", "LogOff", "Account")</li>
            </ul>
    </li>
</div>